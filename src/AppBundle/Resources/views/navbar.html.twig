<nav class="navbar navbar-toggleable-md navbar-inverse bg-red sticky-top">
    <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <a class="navbar-brand text-center" href="#">Build-Zeitung</a>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="#">Excerpts <span class="sr-only">(current)</span></a>
            </li>
        </ul>
        {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
            <form action="{{ path('excerpt_convert') }}" method="POST" class="form-inline mr-2">
                <input class="form-control mr-sm-2" type="text" name="url" placeholder="New article">
                <button class="btn btn-black my-2 my-sm-0" type="submit">Suggest</button>
            </form>
        {% endif %}
        <span class="navbar-text pb-0">
            {% if app.user %}
                <img src="{{ app.user.profilePicture }}" width="30" height="30" class="d-inline-block align-top rounded-circle" alt="" />
                    {{ app.user.username }}
            {% else %}
                {% include '@App/_loginButton.html.twig' %}
            {% endif %}
        </span>
    </div>
</nav>
