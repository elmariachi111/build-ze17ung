<h2>Upcoming Meetups</h2>
<table class="table table-condensed">
{% for event in events %}
    <tr>
        <td>{{ event.meetupGroup.name }}</td>
        <td>{{ event.time|date }}</td>
        <td><a data-toggle="tooltip" title="{{ event.venueAddress }}">{{ event.venueName }}</a></td>
        <td><a href="{{ event.link }}" target="_blank"><i class="glyphicon glyphicon-circle-arrow-right"></i></a></td>
    </tr>
{% endfor %}
</table>

<p>Hier gelistete Gruppen
<ul class="list-inline pull-left">
    {% for group in groups %}
        <li><a href="//www.meetup.com/{{ group.urlname }}" target="_blank">{{ group.name }}</a></li>
    {% endfor %}
</ul>
</p>
